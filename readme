# Full Stack Project Setup - React + Node.js

Created by **Inus Daimary**

This project is divided into two main folders:

- `frontend` → React (Frontend)
- `backend` → Node.js (Backend)

Follow the steps below to install and run the application.

---

## 📦 Prerequisites
Before starting, make sure you have the following installed:

- [Node.js](https://nodejs.org/) (v16 or higher recommended)
- npm (comes with Node.js) or [yarn](https://classic.yarnpkg.com/en/docs/install)

---mkdir backend
   cd backend
  npm init -y
  npm install express cors
  node index.js
## 🚀 Frontend (React with Vite) Setup

1. Navigate to your project folder (or create one):
   ```bash
   mkdir frontend
   cd frontend
   npm create vite@latest
   npm install
   npm run dev


🔑 Creating a Hugging Face API Key
If your project uses Hugging Face APIs (e.g., for AI/ML tasks), you’ll need an API key:

Go to https://huggingface.co/

Log in or create an account.

Click on your profile picture → Settings → Access Tokens

Click New token → Give it a name → Select Read or Write as needed

Copy the generated API key and save it securely.

Create a .env file in your backend and store your key like this:



HUGGINGFACE_API_KEY=your_api_key_here

